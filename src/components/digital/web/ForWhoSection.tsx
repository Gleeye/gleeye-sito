'use client';

import ForWhoSection from '@/components/subpage/ForWhoSection';

export default function WebForWhoSection() {
  return (
    <ForWhoSection
      items={[
        "Chi ha un sito vecchio che non riflette più l'identità attuale dell'azienda — e sa che ogni visitatore che se ne va è un'occasione persa.",
        'Chi sta lanciando un nuovo prodotto o servizio e ha bisogno di una presenza web professionale da subito, costruita per scalare.',
        'Chi gestisce le campagne ADV ma manda il traffico su pagine che non convertono — e vuole capire dove si rompe il funnel.',
      ]}
    />
  );
}
