'use client';

import SubForWho from '@/components/subpage/ForWhoSection';

export default function SocialForWhoSection() {
  return (
    <SubForWho
      items={[
        "Chi pubblica sui social in modo discontinuo, senza piano, spesso con contenuti generici che non riflettono l'identità dell'azienda.",
        'Chi ha un ufficio marketing interno che non riesce a tenere il ritmo di produzione richiesto da tutti i canali attivi.',
        'Chi sa che i social potrebbero portare clienti, ma non ha mai avuto i risultati che si aspettava — e vuole capire perché.',
      ]}
    />
  );
}
